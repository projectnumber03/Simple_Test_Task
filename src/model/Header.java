package model;

public class Header extends Line {
    public Header(int numberLength, int dateLength, int nameLength, String number, String date, String name) {
        super(numberLength, dateLength, nameLength, number, date, name);
    }
}
